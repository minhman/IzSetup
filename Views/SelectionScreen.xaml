<UserControl x:Class="IzSetup.Views.SelectionScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search and Filter Panel -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <TextBox Grid.Column="0" x:Name="SearchBox"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10" VerticalAlignment="Center"
                         Foreground="#333333" Background="White"
                         BorderThickness="1" BorderBrush="#CCCCCC"/>

                <!-- Category Filter -->
                <ComboBox Grid.Column="1" Margin="10,0,0,0"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          Padding="10" VerticalAlignment="Center"
                          Background="White" BorderThickness="1" BorderBrush="#CCCCCC"/>

                <!-- Control Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                    <Button Command="{Binding SelectAllCommand}" Content="Select All" Padding="10,5" Margin="0,0,5,0" Background="#27AE60" Foreground="White" BorderThickness="0"/>
                    <Button Command="{Binding DeselectAllCommand}" Content="Deselect All" Padding="10,5" Background="#E74C3C" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Selected Count -->
        <TextBlock Grid.Row="1" Text="{Binding SelectedCountText}" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="#0078D4"/>

        <!-- Software List -->
        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding SoftwareList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>

                                <!-- Checkbox -->
                                <CheckBox Grid.Column="0" VerticalAlignment="Center"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay}"/>

                                <!-- Software Info -->
                                <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#333333"/>
                                    <TextBlock Text="{Binding Publisher}" FontSize="11" Foreground="#666666" Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#999999" Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- Category Badge -->
                                <Border Grid.Column="2" CornerRadius="3" Background="#E8F0F7" Padding="8,5" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Category}" FontSize="11" Foreground="#0078D4" TextAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
