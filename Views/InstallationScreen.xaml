<UserControl x:Class="IzSetup.Views.InstallationScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Status Message -->
        <TextBlock Grid.Row="0" Text="{Binding StatusMessage}" FontSize="14" FontWeight="Bold" Foreground="#0078D4" Margin="0,0,0,10"/>

        <!-- Overall Progress -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Overall Progress" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
            <ProgressBar Grid.Row="1" Value="{Binding OverallProgress}" Height="20" Background="#E8E8E8" Foreground="#27AE60"/>
            <TextBlock Grid.Row="1" Text="{Binding OverallProgressText}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="11"/>
        </Grid>

        <!-- Installation Stats -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="{Binding CompletedCount}" FontSize="20" FontWeight="Bold" Foreground="#27AE60" Margin="0,0,5,0"/>
                <TextBlock Text="/" FontSize="20" FontWeight="Bold" Foreground="#999999"/>
                <TextBlock Text="{Binding TotalCount}" FontSize="20" FontWeight="Bold" Foreground="#333333" Margin="5,0,10,0"/>
                <TextBlock Text="Completed" VerticalAlignment="Center" Foreground="#666666"/>
            </StackPanel>
        </Border>

        <!-- Installation Details -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Individual App Progress -->
            <Border Grid.Column="0" BorderThickness="0,0,1,0" BorderBrush="#E0E0E0" Padding="0,0,15,0" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Installation Details" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ScrollViewer Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding Installations}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="0,10">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Text="{Binding Software.Name}" FontWeight="Bold" Foreground="#333333"/>
                                            <ProgressBar Grid.Row="1" Value="{Binding Percentage}" Height="8" Background="#E8E8E8" Foreground="#0078D4" Margin="0,5,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Installation Log -->
            <Border Grid.Column="1" Padding="15,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Installation Log" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>

                    <ScrollViewer Grid.Row="1" Background="#1E1E1E" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Logs}" HorizontalContentAlignment="Stretch">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormattedMessage}" FontSize="10" FontFamily="Courier New" Foreground="#00FF00" Margin="5" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Cancel Button -->
        <Button Grid.Row="4" Command="{Binding CancelInstallationCommand}"
                IsEnabled="{Binding CanCancel}"
                Content="Cancel Installation" Padding="15,8" Margin="0,15,0,0"
                Background="#E74C3C" Foreground="White" BorderThickness="0" HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
