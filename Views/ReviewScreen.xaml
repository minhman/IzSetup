<UserControl x:Class="IzSetup.Views.ReviewScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Summary Section -->
            <Border Grid.Row="0" Background="#E8F0F7" Padding="20" CornerRadius="5" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Applications -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="APPLICATIONS" FontSize="11" Foreground="#0078D4" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalApplications}" FontSize="24" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Total Size -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="TOTAL SIZE" FontSize="11" Foreground="#0078D4" FontWeight="Bold"/>
                        <TextBlock Text="{Binding TotalSizeFormatted}" FontSize="24" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Estimated Time -->
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="ESTIMATED TIME" FontSize="11" Foreground="#0078D4" FontWeight="Bold"/>
                        <TextBlock Text="{Binding EstimatedTime}" FontSize="24" FontWeight="Bold" Foreground="#333333" Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Selected Software List -->
            <TextBlock Grid.Row="1" Text="Selected Software" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                <ItemsControl ItemsSource="{Binding SelectedSoftware}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#E0E0E0" Padding="0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#333333"/>
                                        <TextBlock Text="{Binding Publisher}" FontSize="11" Foreground="#666666" Margin="0,3,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1" Text="{Binding Version}" FontSize="11" Foreground="#999999" VerticalAlignment="Center" TextAlignment="Right"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Terms Agreement -->
            <Border Grid.Row="3" Background="#FFF8DC" BorderThickness="1" BorderBrush="#FFE4B5" Padding="15" CornerRadius="3" Margin="0,15,0,0">
                <StackPanel>
                    <CheckBox IsChecked="{Binding TermsAgreed}">
                        <TextBlock>
                            <Run Text="I agree to the terms of use and understand that this will install the selected software on my system."/>
                        </TextBlock>
                    </CheckBox>
                    <TextBlock Text="⚠ Please review the list above before proceeding." FontSize="11" Foreground="#E74C3C" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
